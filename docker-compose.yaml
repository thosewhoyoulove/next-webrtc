version: '3.8'

services:
  socket:
    build:
      context: ./apps/socket
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: always
